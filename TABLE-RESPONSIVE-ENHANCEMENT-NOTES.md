# Responsive Table Enhancement — v16

- Audited every static HTML table in the website.
- Added one shared responsive table system to all HTML pages, including dynamic product tables.
- Desktop tables remain full width.
- Two-column tables retain their normal table layout through tablet widths and stack on narrow phones.
- Three-or-more-column tables convert to labeled cards below 800 px.
- Long text and URLs wrap inside cells instead of forcing page overflow.
- Print layouts revert to standard tables.
- The five MICE article content checksums match before and after the enhancement.
